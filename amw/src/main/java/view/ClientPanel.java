package view;

import interpackage.RequestDispatcher;
import view.panels.OrderPanel;

public class ClientPanel extends OrderPanel {
	public ClientPanel ( RequestDispatcher dispatcher ) {
		super( dispatcher );
	}
}
